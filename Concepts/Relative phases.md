Any complex number can be written in the form as $re^{i\phi}$ [[(5)]].
- r - magnitude
- $\phi$ - phase factor
In a ket (e.g. $\ket{\psi} = a\ket{0} + b\ket{1}$), relative phase is the difference in between the phases of coefficients $a$ and $b$. 